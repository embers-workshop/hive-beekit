# Simple Responder Example

This example shows the minimal Beekit bot loop:

1. Create a manifest with one `help` command.
2. Start `hive-beekit dev` with your Bluesky credentials.
3. Implement custom message handling inside your app runtime.

Use this as a baseline for PR reviewer / summarizer templates.
